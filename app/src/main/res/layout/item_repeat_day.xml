<?xml version="1.0" encoding="utf-8"?>
<!-- Hlavička dne / oddílu v „Zopakovat jídlo“ (docs/adr/0027). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="16dp"
    android:gravity="center_vertical"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/tvRepeatDay"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:letterSpacing="0.08"
        android:textColor="@color/brand_primary"
        android:textSize="11sp"
        android:textStyle="bold" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnRepeatDayAdd"
        style="@style/Widget.MaterialComponents.Button.TextButton"
        android:layout_width="wrap_content"
        android:layout_height="36dp"
        android:insetTop="0dp"
        android:insetBottom="0dp"
        android:minWidth="0dp"
        android:paddingHorizontal="10dp"
        android:text="Celý den"
        android:textAllCaps="false"
        android:textColor="@color/brand_dark"
        android:textSize="13sp"
        app:icon="@drawable/ic_line_repeat"
        app:iconSize="16dp"
        app:iconTint="@color/brand_dark" />

    <ImageButton
        android:id="@+id/btnRepeatDaySave"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="Uložit celý den jako šablonu"
        android:padding="10dp"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_line_bookmark"
        app:tint="@color/brand_dark" />
</LinearLayout>
