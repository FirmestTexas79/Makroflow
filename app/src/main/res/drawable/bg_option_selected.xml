<?xml version="1.0" encoding="utf-8"?>
<!-- Vybraná volba (životní styl): jemně olivová s obrysem -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android" android:color="@color/brand_primary_alpha20">
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="16dp" />
            <solid android:color="@color/brand_primary_alpha10" />
            <stroke android:width="1.5dp" android:color="@color/brand_primary" />
        </shape>
    </item>
</ripple>
